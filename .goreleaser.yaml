version: 2

builds:
  - id: karpenter-provider-huawei
    main: ./cmd/controller
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64

archives: []

release:
  discussion_category_name: General
  draft: true
